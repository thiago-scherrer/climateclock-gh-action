# action.yml
name: 'thiago-scherrer/climateclock-g-action'
description: 'Action to update markdown with climateclock.world'
runs:
  using: "composite"
  steps:
    - run: docker run ubuntu-latest
      shell: bash